{
  "name": "vima-sneha-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build:admin-ui": "node scripts/generate-admin-ui.js",
    "deploy": "npm run build:admin-ui && wrangler deploy",
    "check": "node --check src/index.js"
  },
  "dependencies": {
    "hono": "^4.5.8"
  },
  "devDependencies": {
    "wrangler": "^3.114.17"
  }
}
